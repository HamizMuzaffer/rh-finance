generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model admin {
  id       Int     @id @default(autoincrement())
  email    String?
  password String?
}